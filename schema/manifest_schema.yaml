$schema: "https://json-schema.org/draft/2020-12/schema"
description: an entry in the sample manifest
properties:
  vcf:
    type: string
    description: path to vcf containing variants calls
    pattern: "^.*\\.vcf\\.gz$"
required:
  - vcf
additionalProperties: no
